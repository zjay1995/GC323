

//////////////////////// bradshaw autozero experiment  12/13/2020

dword dwAutoZeroTime;
AutoZero(TRUE);
logit("starting 11 seconds of AutoZero");
dwAutoZeroTime=GetTickCount()+11*1000UL; 
if(dwAutoZeroTime=GetTickCount()<GetTicCount());
AutoZero(FALSE); 
logit("AutoZerio should be done, 11 seconds later");

//////////////////////// bradshaw autozero experiment 12/13/2020

